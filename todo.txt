* intergrate with XSF
	* buildbot tests
	* simple init
	* performance tests
	* include tracebacks?
	* srcgen: parse consts

* features:
	* async, timed
	* SSL

* targets:
	1 powershell
	2 c++
	3 REST access in the meanwhile:
	    can implement REST on top of agnos, by writing a small python mediator 
	    program that acts as a client from one side, and exposes REST 
	    web access on the other
	4 jscript client (over URLs) for web gui

* optional features:
    * by-ref list, by-ref map
    * custom type templates
    * complex consts

* maybe targets:
    * c
    * perl
    * php

