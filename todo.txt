* todo:
    * finish RESTful
    * !!!! add INFO_TYPES, INFO_SERVICE to all targets
    * java manifest (minimum version?)
    * c# assembly info
    * site2

* features:
	* async, timed
	* rlist, rset, rmap (by ref)
    * XML RPC backend?
    * serialize to file -- can be used as a universal serializer
    * java srcgen
    * c# srcgen

* targets:
	* powershell
	* jscript client (over URLs) for web gui

* optional features:
    * custom type templates
    * complex consts
	* always include tracebacks?

* maybe targets:
    * c
    * perl
    * php
    * haskell


